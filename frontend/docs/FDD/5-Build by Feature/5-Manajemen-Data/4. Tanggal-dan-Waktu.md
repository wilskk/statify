# Pembangunan Fitur: Tanggal & Waktu

Dokumen ini berisi laporan pembangunan (build report) untuk Feature Set `Tanggal & Waktu`.

---

## 1. Work Package

- **Fitur yang Dibangun:**
  - Mengimplementasikan wizard `DefineDateTime` untuk memandu pengguna membuat variabel tanggal/waktu.
  - Membuat `dateTimeService` untuk menampung logika bisnis manipulasi tanggal.
  - Mengintegrasikan library `date-fns` untuk parsing dan pemformatan tanggal yang andal.
  - Menambahkan aksi ke data store untuk memasukkan variabel dan data tanggal yang baru dibuat.
- **Tim Fitur (Class Owners):**
  - `[Nama Tim/Developer]`
- **Tanggal Aktual:**
  - **Pembangunan:** `[Tanggal Mulai]` - `[Tanggal Selesai]`
  - **Unit Testing:** `[Tanggal Selesai]`
  - **Inspeksi Kode:** `[Tanggal Selesai]`

---

## 2. Implementation

- **Lokasi Kode Sumber Utama:**
  - `frontend/components/Modals/Data/DefineDateTime/`: Direktori yang berisi komponen UI untuk wizard.
  - `frontend/services/data/dateTimeService.ts`: Service yang berisi logika murni untuk operasi tanggal.
  - `frontend/utils/dateTimeFormatters.ts`: Utilitas untuk daftar format tanggal yang didukung.
- **Dependensi Kunci:**
  - `date-fns`: Library untuk manipulasi tanggal dan waktu.

---

## 3. Unit Testing

- **Framework & Tools:**
  - **Jest:** Test runner.
  - **React Testing Library:** Untuk rendering dan interaksi dengan komponen UI.
  - **Zustand Test Utils:** Untuk mock data stores.

- **Skenario Pengujian:**
  - **File:** `DefineDateTime.test.tsx`, `dateTimeService.test.ts`
  - **Deskripsi:** Pengujian ini memastikan bahwa wizard tanggal/waktu berfungsi dengan benar, terutama pada logika parsing dan kalkulasi di dalam service.

| Test Case ID | Deskripsi | Aksi | Hasil yang Diharapkan | Status |
| :--- | :--- | :--- | :--- | :--- |
| **DT-UI-1** | Render Wizard | Render komponen `DefineDateTime`. | Wizard ditampilkan dengan langkah awal. | `Passed` |
| **DT-SVC-1** | Parsing dari String (DMY) | Panggil `createDateVariable` dengan format `dd/MM/yyyy`. | String tanggal seperti '27/08/2024' di-parse dengan benar. | `Passed` |
| **DT-SVC-2** | Parsing dari String (MDY) | Panggil `createDateVariable` dengan format `MM-dd-yy`. | String tanggal seperti '08-27-24' di-parse dengan benar. | `Passed` |
| **DT-SVC-3** | Kalkulasi Durasi | Panggil fungsi untuk menghitung durasi antara dua variabel tanggal. | Kolom baru berisi selisih waktu yang benar (misal, dalam hari). | `Passed` |
| **DT-SVC-4**| Penanganan Format Invalid | Panggil service dengan string tanggal yang tidak cocok format. | Service menangani error dengan baik (misal, mengembalikan `null` atau `Invalid Date`). | `Passed` |
| **DT-STORE-1**| Penambahan Variabel Baru | Proses wizard selesai dengan sukses. | Variabel tanggal baru dan datanya ditambahkan ke store masing-masing. | `Passed` |

---

## 4. Laporan Inspeksi Kode (Code Inspection Report)

- **Hasil:** `[Lulus / Lulus dengan Catatan / Gagal]`
- **Catatan Review:**
  - `[Catatan dari reviewer 1]`
  - `[Catatan dari reviewer 2]`

---

## 5. Status Promosi ke Build

- **Status:** `[Siap / Belum Siap]`
- **Catatan:**
  - `[Catatan mengenai kesiapan fitur untuk diintegrasikan ke dalam build utama]` 