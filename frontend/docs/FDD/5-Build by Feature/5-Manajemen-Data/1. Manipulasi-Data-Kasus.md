# Pembangunan Fitur: Manipulasi Data (Kasus)

Dokumen ini berisi laporan pembangunan (build report) untuk Feature Set `Manipulasi Data (Kasus)`.

---

## 1. Work Package

- **Fitur yang Dibangun:**
  - Mengimplementasikan UI untuk memilih variabel pengurutan dan arahnya (naik/turun).
  - Membuat web worker untuk menjalankan proses pengurutan data di latar belakang.
  - Mengintegrasikan service data untuk berkomunikasi dengan worker dan memperbarui state di `useDataStore`.
- **Tim Fitur (Class Owners):**
  - `[Nama Tim/Developer]`
- **Tanggal Aktual:**
  - **Pembangunan:** `[Tanggal Mulai]` - `[Tanggal Selesai]`
  - **Unit Testing:** `[Tanggal Selesai]`
  - **Inspeksi Kode:** `[Tanggal Selesai]`

---

## 2. Implementation

- **Lokasi Kode Sumber Utama:**
  - `frontend/components/Modals/Data/SortCases/SortCasesUI.tsx`: Komponen UI untuk dialog pengurutan kasus.
  - `frontend/stores/useDataStore.ts`: Mengandung aksi (`sortCases`) yang mem-post pesan ke worker.
  - `frontend/public/workers/sortCases.worker.js`: Web worker yang berisi logika pengurutan kasus.

---

## 3. Unit Testing

- **Framework & Tools:**
  - **Jest:** Test runner.
  - **React Testing Library:** Untuk rendering dan interaksi dengan komponen UI.
  - **Zustand Test Utils:** Untuk mock `useDataStore`.

- **Skenario Pengujian:**
  - **File:** `SortCasesUI.test.tsx`, `useDataStore.test.ts`, `sortCases.worker.test.js`
  - **Deskripsi:** Pengujian ini memverifikasi fungsionalitas pengurutan kasus, dari interaksi pengguna di UI, komunikasi dengan worker, hingga pembaruan state data.

| Test Case ID | Deskripsi | Aksi | Hasil yang Diharapkan | Status |
| :--- | :--- | :--- | :--- | :--- |
| **SORT-UI-1** | Render komponen UI | Render `SortCasesUI`. | Komponen ditampilkan dengan daftar variabel yang tersedia. | `Passed` |
| **SORT-UI-2** | Pemilihan variabel urutan | Pengguna memilih variabel dan menambahkannya ke daftar urutan. | Variabel muncul di daftar 'Urutkan Berdasarkan'. | `Passed` |
| **SORT-STORE-1**| Panggilan aksi `sortCases`| Panggil `useDataStore.getState().sortCases(config)`. | Pesan dikirim ke `sortCases.worker.js` dengan data dan konfigurasi. | `Passed` |
| **SORT-WORKER-1**| Pengurutan Menaik | Worker menerima data dan config untuk urutan menaik. | Worker mengembalikan data yang diurutkan dengan benar (A-Z, 1-10). | `Passed` |
| **SORT-WORKER-2**| Pengurutan Menurun | Worker menerima data dan config untuk urutan menurun. | Worker mengembalikan data yang diurutkan dengan benar (Z-A, 10-1). | `Passed` |
| **SORT-WORKER-3**| Pengurutan Multi-Level | Worker menerima config untuk urutan berdasarkan beberapa variabel. | Data diurutkan sesuai prioritas variabel. | `Passed` |
| **SORT-STORE-2**| Pembaruan State | Worker mengirim kembali data terurut. | Aksi di `useDataStore` memperbarui state dengan data baru. | `Passed` |

---

## 4. Laporan Inspeksi Kode (Code Inspection Report)

- **Hasil:** `[Lulus / Lulus dengan Catatan / Gagal]`
- **Catatan Review:**
  - `[Catatan dari reviewer 1]`
  - `[Catatan dari reviewer 2]`

---

## 5. Status Promosi ke Build

- **Status:** `[Siap / Belum Siap]`
- **Catatan:**
  - `[Catatan mengenai kesiapan fitur untuk diintegrasikan ke dalam build utama]` 