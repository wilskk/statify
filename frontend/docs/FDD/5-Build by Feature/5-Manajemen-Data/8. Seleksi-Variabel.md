# Pembangunan Fitur: Seleksi & Pengurutan Variabel

Dokumen ini berisi laporan pembangunan (build report) untuk Feature Set `Seleksi & Pengurutan Variabel`.

---

## 1. Work Package

- **Fitur yang Dibangun:**
  - Mengimplementasikan dialog untuk mengubah level pengukuran (Nominal, Ordinal, Scale) untuk variabel yang dipilih.
  - Mengimplementasikan dialog untuk mengurutkan ulang variabel dalam dataset berdasarkan atributnya (misalnya, Nama, Tipe, Label).
  - Menyimpan urutan variabel kustom dalam `useMetaStore` tanpa mengubah urutan fisik array variabel.
- **Tim Fitur (Class Owners):**
  - `[Nama Tim/Developer]`
- **Tanggal Aktual:**
  - **Pembangunan:** `[Tanggal Mulai]` - `[Tanggal Selesai]`
  - **Unit Testing:** `[Tanggal Selesai]`
  - **Inspeksi Kode:** `[Tanggal Selesai]`

---

## 2. Implementation

- **Lokasi Kode Sumber Utama:**
  - `frontend/components/Modals/Edit/SetMeasurementLevel/`: Komponen UI untuk mengatur level pengukuran.
  - `frontend/components/Modals/Edit/SortVars/`: Komponen UI untuk dialog pengurutan variabel.
  - `frontend/stores/useMetaStore.ts`: Berisi aksi untuk menerapkan perubahan level pengukuran dan untuk mengatur ulang array urutan variabel.

---

## 3. Unit Testing

- **Framework & Tools:**
  - **Jest:** Test runner.
  - **React Testing Library:** Untuk rendering dan interaksi dengan komponen UI.
  - **Zustand Test Utils:** Untuk mock `useMetaStore`.

- **Skenario Pengujian:**
  - **File:** `SetMeasurementLevel.test.tsx`, `SortVars.test.tsx`, `useMetaStore.test.ts`
  - **Deskripsi:** Pengujian ini memverifikasi bahwa operasi metadata pada variabel, seperti mengubah level pengukuran dan urutan, diterapkan dengan benar di dalam store.

| Test Case ID | Deskripsi | Aksi | Hasil yang Diharapkan | Status |
| :--- | :--- | :--- | :--- | :--- |
| **VAR-UI-1** | Render UI Level Pengukuran | Render `SetMeasurementLevel` modal. | UI untuk memilih variabel dan level pengukuran ditampilkan. | `Passed` |
| **VAR-STORE-1**| Terapkan Level Pengukuran | Panggil aksi untuk mengubah level pengukuran beberapa variabel. | Properti `measure` pada variabel yang dipilih di `useMetaStore` diperbarui. | `Passed` |
| **VAR-UI-2** | Render UI Urutkan Variabel | Render `SortVars` modal. | UI untuk memilih kriteria pengurutan ditampilkan. | `Passed` |
| **VAR-STORE-2**| Urutkan Berdasarkan Nama | Jalankan aksi untuk mengurutkan berdasarkan nama (A-Z). | Array `variableOrder` di `useMetaStore` berisi nama variabel dalam urutan alfabetis. | `Passed` |
| **VAR-STORE-3**| Urutkan Berdasarkan Tipe | Jalankan aksi untuk mengurutkan berdasarkan tipe. | Array `variableOrder` diurutkan berdasarkan tipe variabel (misalnya, numerik dulu, lalu string). | `Passed` |

---

## 4. Laporan Inspeksi Kode (Code Inspection Report)

- **Hasil:** `[Lulus / Lulus dengan Catatan / Gagal]`
- **Catatan Review:**
  - `[Catatan dari reviewer 1]`
  - `[Catatan dari reviewer 2]`

---

## 5. Status Promosi ke Build

- **Status:** `[Siap / Belum Siap]`
- **Catatan:**
  - `[Catatan mengenai kesiapan fitur untuk diintegrasikan ke dalam build utama]` 