# Pembangunan Fitur: Pembobotan Kasus

Dokumen ini berisi laporan pembangunan (build report) untuk Feature Set `Pembobotan Kasus`.

---

## 1. Work Package

- **Fitur yang Dibangun:**
  - Mengimplementasikan dialog untuk memilih atau menonaktifkan variabel pembobot.
  - Menerapkan validasi untuk memastikan hanya variabel numerik yang dapat digunakan sebagai bobot.
  - Menyimpan nama variabel pembobot yang aktif sebagai state global di `useMetaStore`.
  - Memastikan prosedur analisis lain dapat mengakses dan menggunakan variabel pembobot ini.
- **Tim Fitur (Class Owners):**
  - `[Nama Tim/Developer]`
- **Tanggal Aktual:**
  - **Pembangunan:** `[Tanggal Mulai]` - `[Tanggal Selesai]`
  - **Unit Testing:** `[Tanggal Selesai]`
  - **Inspeksi Kode:** `[Tanggal Selesai]`

---

## 2. Implementation

- **Lokasi Kode Sumber Utama:**
  - `frontend/components/Modals/Data/WeightCases/`: Direktori yang berisi komponen UI.
  - `frontend/hooks/useWeightCases.ts`: Hook yang menangani logika UI dan validasi.
  - `frontend/stores/useMetaStore.ts`: Berisi state `weightVariable` dan aksi untuk mengatur/menghapusnya.

---

## 3. Unit Testing

- **Framework & Tools:**
  - **Jest:** Test runner.
  - **React Testing Library:** Untuk rendering dan interaksi dengan komponen UI.
  - **Zustand Test Utils:** Untuk mock `useMetaStore`.

- **Skenario Pengujian:**
  - **File:** `WeightCases.test.tsx`, `useMetaStore.test.ts`
  - **Deskripsi:** Pengujian ini memverifikasi bahwa state pembobotan global dapat diatur dan dihapus dengan benar. Verifikasi penggunaan bobot dilakukan dalam pengujian untuk prosedur analisis individual.

| Test Case ID | Deskripsi | Aksi | Hasil yang Diharapkan | Status |
| :--- | :--- | :--- | :--- | :--- |
| **WGT-UI-1** | Render UI | Render `WeightCasesModal`. | Dropdown hanya menampilkan variabel numerik dari `useMetaStore`. | `Passed` |
| **WGT-UI-2** | Validasi UI | Coba pilih variabel non-numerik (jika dimungkinkan oleh mock). | UI harus mencegah pemilihan atau menampilkan pesan error. | `Passed` |
| **WGT-STORE-1**| Atur Variabel Pembobot | Panggil aksi `setWeightVariable` dengan nama variabel yang valid. | State `weightVariable` di `useMetaStore` diatur ke nama variabel tersebut. | `Passed` |
| **WGT-STORE-2**| Nonaktifkan Pembobotan | Panggil aksi `setWeightVariable` dengan `null` atau string kosong. | State `weightVariable` di `useMetaStore` menjadi `null`. | `Passed` |
| **WGT-INT-1** | (Integrasi) Penggunaan Bobot | Jalankan analisis Frekuensi saat bobot aktif. | Hasil frekuensi (misalnya, N) mencerminkan nilai dari variabel pembobot, bukan hitungan baris sederhana. | `Passed` |

---

## 4. Laporan Inspeksi Kode (Code Inspection Report)

- **Hasil:** `[Lulus / Lulus dengan Catatan / Gagal]`
- **Catatan Review:**
  - `[Catatan dari reviewer 1]`
  - `[Catatan dari reviewer 2]`

---

## 5. Status Promosi ke Build

- **Status:** `[Siap / Belum Siap]`
- **Catatan:**
  - `[Catatan mengenai kesiapan fitur untuk diintegrasikan ke dalam build utama]` 