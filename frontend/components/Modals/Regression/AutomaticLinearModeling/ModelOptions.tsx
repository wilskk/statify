// components/Modals/Regression/AutomaticLinearModeling/ModelOptions.tsx

import React from 'react';

const ModelOptions: React.FC = () => {
    return (
        <div className="p-4">
            {/* Tambahkan konten Model Options di sini */}
            <h2 className="text-lg font-semibold mb-2">Model Options</h2>
            <p>Model Options content goes here.</p>
            {/* Misalnya, pengaturan model, parameter, dll. */}
        </div>
    );
};

export default ModelOptions;
